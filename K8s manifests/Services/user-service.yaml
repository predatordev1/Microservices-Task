apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: microservices
spec:
  type: NodePort
  selector:
    app: user
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000